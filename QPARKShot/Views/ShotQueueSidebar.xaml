<UserControl
    x:Class="QPARKShot.Views.ShotQueueSidebar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource SidebarFill}" BorderBrush="{DynamicResource DividerStroke}"
            BorderThickness="0,0,1,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,8">
                <TextBlock x:Name="HeaderText" Text="Buffer · 0" FontWeight="SemiBold" FontSize="11"
                           VerticalAlignment="Center" />
                <Button x:Name="ClearAllButton" Click="OnClearAll" Style="{StaticResource IconButton}"
                        Margin="6,0,0,0" ToolTip="Clear buffer" Visibility="Collapsed">
                    <TextBlock Text="🗑" FontSize="10" />
                </Button>
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="QueueItems" Margin="8" />
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
