name: Build Windows

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      # Use the pre-installed Visual Studio + .NET 8 SDK on the runner —
      # it ships with the AppxPackage / WindowsAppSDK MSBuild targets that
      # `setup-dotnet`'s slim SDK doesn't include. global.json pins to 8.x.
      - name: Show active SDK
        run: |
          dotnet --info | Select-String -Pattern "Version|RID" -SimpleMatch
          dotnet --list-sdks

      - name: Restore
        run: dotnet restore QPARKShot.sln

      - name: Build (Release)
        run: pwsh ./scripts/build_release.ps1

      - name: List build output (debug)
        run: |
          Write-Host "==> build/Release contents:"
          if (Test-Path build/Release) {
            Get-ChildItem -Recurse build/Release | Select-Object FullName, Length | Format-Table -AutoSize | Out-String -Width 200
          } else {
            Write-Host "MISSING"
          }

      - name: Upload Release build (unpackaged)
        uses: actions/upload-artifact@v4
        with:
          name: QPARKShot-Windows-Release
          path: build/Release/**
          if-no-files-found: error
